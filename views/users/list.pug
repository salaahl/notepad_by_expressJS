extends ../base

append stylesheets

block content
    h1
        for letter of title
            span=letter

    main
        ul
            for user in users
                li=user.fullname + ', ' + user.email
